services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: drugtarget
      POSTGRES_PASSWORD: drugtarget
      POSTGRES_DB: drugtarget
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: